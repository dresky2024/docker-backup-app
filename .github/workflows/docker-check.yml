name: Docker Build Test
on: [push] # Запуск при каждом пуше в любую ветку
jobs:
  check-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Try to build image
        run: docker build . --file Dockerfile --tag my-test-app:latest
